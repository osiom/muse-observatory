#CREATE DATABASE muse_observatory;
#CREATE USER oracle WITH PASSWORD 'oracle';
#GRANT ALL PRIVILEGES ON DATABASE muse_observatory TO oracle;

# Login to the postgresdb
docker exec -it 9eaa0ae20151 psql -U museuser -d muse_observatory

# tables
\dt
# table
\d <table-name>

# check structure table
\d daily_facts
\d inspirations

## mos.local
sudo su
su - postgres
psql muse_observatory

